import 'package:flutter/material.dart';

class AppColors {

    AppColors._();

    // Backgrounds
    static const Color background = Color(0xFF0F1117);

    static const Color surface = Color(0xFF1A1D26);

    static const Color surfaceVariant = Color(0xFF252836);

    // Accents
    static const Color workGreen = Color(0xFF4CAF50);

    static const Color distractionOrange = Color(0xFFFF9800);

    static const Color learningBlue = Color(0xFF2196F3);

    static const Color pauseGray = Color(0xFF616161);

    // Text
    static const Color textPrimary = Color(0xFFFFFFFF);

    static const Color textSecondary = Color(0xFF9E9E9E);

    static const Color textDisabled = Color(0xFF424242);

    // Overtime
    static const Color overtimeRed = Color(0xFFEF5350);

    // Success
    static const Color successGold = Color(0xFFFFD700);
}
